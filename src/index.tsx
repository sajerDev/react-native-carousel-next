import * as React from 'react';
import {SafeAreaView , Text } from 'react-native';

const Carousel = ({ example }: any) => {
    return(
        <SafeAreaView>
            <Text>{ example } ss</Text>
        </SafeAreaView>
    );
};

export { Carousel }
